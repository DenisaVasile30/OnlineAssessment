{% extends 'base.html.twig' %}


{% block body %}

    <div>
        <a href="{{ path('app_add_subject') }}">
            Add new subject
        </a>
    </div>

    <div>
        <a href="{{ path('app_schedule_assessment') }}">
            Schedule assessment
        </a>
    </div>

{% endblock %}
